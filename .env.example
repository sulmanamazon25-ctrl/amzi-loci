# Server A (Fastify API) — local dev
PORT=3000
NODE_ENV=development

# Database (Server B Postgres)
DATABASE_URL=postgresql://amzi_loci:CHANGE_ME@localhost:5432/amzi_loci

# Redis (Server B — used in Phase 4+)
REDIS_URL=redis://localhost:6379

# Desktop app — points at Server A
VITE_SERVER_A_URL=http://localhost:3000

# Server B infra (docker-compose only — set in Coolify UI for production)
POSTGRES_USER=amzi_loci
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=amzi_loci

# Production API (Server A Coolify)
# VITE_SERVER_A_URL=https://amzi-loci.46-62-226-89.sslip.io
