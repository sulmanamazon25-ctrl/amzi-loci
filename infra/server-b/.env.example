# Server B — Postgres + Redis
# Deploy via Coolify Docker Compose on 194.9.62.143 (EURONODE)
#
# Local dev: verify-phase0.ps1 creates infra/server-b/.env with password amzi_loci_dev

POSTGRES_USER=amzi_loci
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=amzi_loci
